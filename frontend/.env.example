# Base URL untuk backend API.
# Saat development dengan Vite proxy, biarkan '/api' (default).
# Untuk production, isi dengan URL backend absolut, misal: https://api.flowops.company.com/api
VITE_API_URL=/api

# Nama aplikasi yang ditampilkan di <title> dan metadata
VITE_APP_NAME=FlowOps
